---
import BackgroundGradient from '@/components/shared/BackgroundGradient.astro';
import CustomImage from '@/components/shared/CustomImage.astro';

export interface CategoryImageProps {
  backgroundImage: Promise<{ default: ImageMetadata }>;
  description?: string;
}

type Props = CategoryImageProps;

const { backgroundImage, description } = Astro.props;
---

<div class='category-image'>
  <div class='category-image__background'>
    <CustomImage src={backgroundImage} alt='image' data-parallax />
  </div>
  <BackgroundGradient class='category-image__gradient' />
  <div class='category-image__content'>
    {description && <p class='category-image__description'>{description}</p>}
    <slot />
  </div>
</div>

<style lang='scss'>
  .category-image {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 100%;
    border-radius: 12px;
    overflow: hidden;
    background-color: black;

    @include media('md') {
      border-radius: 0;
    }

    @include media('lg') {
      border-radius: 12px;
      aspect-ratio: 621 / 768;
    }

    @include customMedia(1600px) {
      border-radius: 16px;
      aspect-ratio: 940 / 896;
    }

    &__background {
      position: relative;
      aspect-ratio: 359 / 524;
      width: 100%;

      img {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
    }

    &__gradient {
    }

    &__content {
      position: relative;
      z-index: 10;
      padding: 16px 12px;
      width: 100%;
      margin-top: -20%;

      @include media('md') {
        padding: 16px 40px 40px;
      }
    }

    &__description {
      font-family: var(--font-secondary);
      font-weight: 500;
      font-style: italic;
      font-size: 14px;
      line-height: 14px;
      text-align: center;
      margin-bottom: 12px;
      color: rgba(255, 255, 255, 0.48);

      @include media('md') {
        margin-bottom: 16px;
      }

      @include media('lg') {
        display: none;
      }
    }
  }
</style>

<script>
  // @ts-ignore
  import SimpleParallax from 'simple-parallax-js/vanilla';

  const items = document.querySelectorAll<HTMLDivElement>('[data-parallax]');
  new SimpleParallax(items, { orientation: 'down', overflow: true, delay: 0, maxTransition: 160, scale: 1.5 });
</script>
